spring.application.name=DevelopmentGr54FS

# ------H2 (for tests, lesson before 7)--------
#spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
#spring.datasource.username=sa
#spring.datasource.password=
#spring.datasource.driver-class-name=org.h2.Driver
#spring.jpa.defer-datasource-initialization=true # it was commented out in lesson 5-6

# ------ PostgreSQL (lesson 7-8) ---------
spring.datasource.url=jdbc:postgresql://localhost:5432/Backend_Pro_Cars
spring.datasource.username=postgres
spring.datasource.password=qwerty007
spring.datasource.driver-class-name=org.postgresql.Driver

# ---- JPA ---------
#spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ----- Console H2 ---------
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ----- Liquibase ---------
spring.liquibase.enabled=true
spring.liquibase.change-log=classpath:db/changelog/db.changelog-master.xml

# ----- Email ---------
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

app.mail.from.adress=${MAIL_USERNAME}
app.mail.from.personal=AIT_Cars

# ----- Files uploading ---------
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

upload.dir=./img/cars
spring.web.resources.static-locations=file:${upload.dir}

host.url=http://localhost:8080/

messages.hello=Hello from local application!